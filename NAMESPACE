useDynLib(Streamer, .registration=TRUE)

import(methods)

exportPattern("^[^\\.]")

export(connect)
exportClasses(Streamer, Producer, Consumer, Stream,
              ConnectionProducer, RawInput, Downsample,
              RawToChar, Rev, ParallelConnector,
              TConnector, YConnector)
